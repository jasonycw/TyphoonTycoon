define([
	'jquery',
	'underscore'
], function($, _) {

	console.log("utility.js loaded");

	var Utility = {
		// Just an example, remove it when you create a new method.
		test: function() {
			return true;
		}
	};

	return Utility;
});